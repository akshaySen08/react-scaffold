import {input,select} from '@inquirer/prompts';import type {Command} from 'commander';import {createTypePlan} from '../generators/type.generator.js';import {context,execute,features} from './shared.js';
export function registerType(p:Command){p.command('type [name]').description('Generate a TypeScript type').option('--feature <name>','target feature').option('--shared','shared type').option('--cwd <path>','starting directory','.').option('--force','overwrite collisions').action(async(name,o)=>{const x=await context(o.cwd),n=name??await input({message:'Type name'});let feature=o.feature;if(!feature&&!o.shared){const target=await select({message:'Scope',choices:[{name:'Feature-specific',value:'feature'},{name:'Shared',value:'shared'}]});if(target==='feature'){const all=await features(x.root,x.config.featuresDir);if(!all.length)throw new Error('No features found.');feature=await select({message:'Feature',choices:all.map(value=>({value}))})}}await execute(`type: ${n}`,createTypePlan(x.root,x.config,{name:n,feature}),o.force)})}
