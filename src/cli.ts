#!/usr/bin/env node
import {Command} from 'commander';import pc from 'picocolors';import {registerInit} from './commands/init.command.js';import {registerFeature} from './commands/feature.command.js';import {registerPage} from './commands/page.command.js';import {registerComponent} from './commands/component.command.js';import {registerHook} from './commands/hook.command.js';import {registerType} from './commands/type.command.js';
const program=new Command().name('react-scaffold').description('Generate scalable, feature-based React TypeScript files').version('0.1.0').showHelpAfterError();registerInit(program);registerFeature(program);registerPage(program);registerComponent(program);registerHook(program);registerType(program);program.parseAsync().catch(error=>{console.error(pc.red(`✖ ${error instanceof Error?error.message:String(error)}`));if(process.env.DEBUG&&error instanceof Error)console.error(error.stack);process.exitCode=1});
