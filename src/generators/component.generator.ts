import path from 'node:path';import type {ScaffoldConfig} from '../config/config.types.js';import {PlanBuilder} from '../utils/generation-plan.js';import {kebabCase,pascalCase,validateName} from '../utils/naming.js';import {componentTemplate} from '../templates/component.template.js';import {cssModuleTemplate} from '../templates/css-module.template.js';import {testTemplate} from '../templates/test.template.js';
export type ComponentTarget='ui'|'layout'|'feature';
export function createComponentPlan(root:string,c:ScaffoldConfig,o:{name:string;target:ComponentTarget;feature?:string;css?:boolean;test?:boolean}){validateName(o.name);if(o.target==='feature'&&!o.feature)throw new Error('A feature is required for a feature component.');if(o.feature)validateName(o.feature);const n=pascalCase(o.name),dir=o.target==='feature'?path.join(c.featuresDir,kebabCase(o.feature!),'components',n):path.join(c.componentsDir,o.target,n),b=new PlanBuilder(root);b.add(path.join(dir,`${n}.tsx`),componentTemplate(n,!!o.css)).removeFile(path.join(path.dirname(dir),'.gitkeep'));if(o.css)b.add(path.join(dir,`${n}.module.css`),cssModuleTemplate());if(o.test)b.add(path.join(dir,`${n}.test.tsx`),testTemplate(n));return b.build()}
